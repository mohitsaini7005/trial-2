import 'package:flutter/material.dart';

class LoveIcon{
  final Icon normal = const Icon(Icons.favorite_border);
  final Icon bold = const Icon(Icons.favorite);
}

class BookmarkIcon{
  final Icon normal = const Icon(Icons.bookmark_border);
  final Icon bold = const Icon(Icons.bookmark);
}
import 'package:flutter/material.dart';

class LockIcon {
  final Icon lock = const Icon(Icons.lock);
  final Icon lockOn = const Icon(Icons.lock);
  final Icon lockOff = const Icon(Icons.lock_open);
}